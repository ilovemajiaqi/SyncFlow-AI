# 截图占位说明

将 README 中引用的截图放到当前目录即可自动显示。

建议文件名：

- `home.png`：首页总览
- `timeline.png`：时间线视图
- `settings.png`：设置中心
- `input.png`：自然语言输入区

建议截图规范：

- 优先使用 `PNG`
- 宽度尽量保持一致，方便 README 排版
- 推荐截取完整页面，避免界面元素被裁断
- 如果是手机界面，尽量保持相同设备比例

如果暂时没有正式截图，也可以先放同名占位图，后续直接覆盖即可。
